# Ревью: правки скриптов — 2026-07-28

**Что ревьюили:** незакоммиченные изменения (`git diff HEAD` + новые `ocrcli.py`,
`pyrightconfig.json`) — 6 изменённых файлов, 2 новых, ~1670 строк ·
**Режим:** с замыслом

**Слои:** слепой-охотник, охотник-за-краями, разрыв-проверяемости, границы-скиллов,
ревьюер-скриптов, аудитор-замысла · **Не отработали:** — ·
**Пропущен:** методический-аудитор (по своему `when`: в изменениях нет учебных файлов)

**Итог:** 6 решений · 16 правок · 9 отложено · 2 отсеяно как шум
**Статус:** все решения приняты, все правки применены и проверены (2026-07-28). Открытых пунктов нет — открытым остаётся только `отложено.md`.

---

## Требуют решения — приняты

- [x] **Читающие команды пишут `progress.json` и затирают `.bak`** ·
      `tracker.py:1378-1382` · важность: **высокая**
      `if args.writes or merged: save(...)` — при сработавшей NFC-миграции `due`,
      `topics`, `progress`, `mistakes`, `drill-plan`, `blockers` (все шесть с
      `writes=False`) переписывают трекер, обновляют `meta.updated` и **перезаписывают
      `.bak`** — ту самую «последнюю целую копию», на которую ссылается сообщение об
      ошибке в `load`. До правки мутация оставалась в памяти.
      Варианты: (а) не сохранять из читающих команд, нормализацию делать только в
      пишущих; (б) сохранять, но не трогать `.bak` при миграции; (в) отдельная команда
      `migrate`, а чтение оставить чтением.

- [x] **Порог Python поднят до 3.9 против шести документов** ·
      `tracker.py:5`, `brain.py:32`, `memlog.py:67`, `lookup.py:22`, `ocrcli.py:14`,
      `pyrightconfig.json` · важность: средняя
      «3.8+» записано как ограничение проекта в шести местах: `README.md:129`,
      `skill-doc-standards.md:95`, `progress-and-spiral.md:14`,
      `skill-brainstorm/SKILL.md:49`, `skill-brainstorm/ATTRIBUTION.md:11` и
      `skill-review/references/review-layers.md:196` — последний прямо велит слою ревью
      ловить «требование Python новее 3.8». То есть правка вносит дефект, который
      собственный слой ревью обязан находить.
      Замерено: 3.9 реально требуют только `brain.py` (3 конструкции) и `trocr.py` (1) —
      `functools.cache` и модульный `Row = dict[str, str]`. В `tracker.py`, `memlog.py`,
      `lookup.py`, `ocrcli.py` — **ноль** 3.9-конструкций, пометка там ложная.
      Варианты: (а) откатить код на 3.8 (`lru_cache(maxsize=None)`, `Row` под
      `TYPE_CHECKING`) и вернуть заголовки; (б) двинуть стандарт на 3.9 во всех шести
      документах одной правкой; (в) 3.9 только для `brain.py`/`trocr.py`, остальным
      вернуть 3.8.

- [x] **`extraPaths` прибит к `python3.14`, результат «0 ошибок» непереносим** ·
      `pyrightconfig.json` · важность: средняя
      `.venv` в `.gitignore`; на свежем клоне или после пересборки venv на другой
      минорной версии путь исчезает молча — pyright не считает несуществующий
      `extraPaths` ошибкой, а `trocr.py` заливается ошибками импорта. Плюс
      `pythonVersion: 3.9` при venv на 3.14: `trocr.py` проверяется под версию, на
      которой не запускается вовсе (torch там нет).
      Варианты: (а) `executionEnvironments` с разными версиями для
      `thai-handwriting/scripts` и остального; (б) вывести `trocr.py` из проверки;
      (в) оставить как есть, дописав предупреждение рядом с уже имеющимся.

- [x] **Проверка типов нигде не объявлена как шаг** · `pyrightconfig.json` ·
      важность: средняя
      Ни `AGENTS.md`, ни карта скиллов, ни один `SKILL.md` не упоминают pyright: кто
      его гоняет, какой командой, обязан ли он проходить перед коммитом — не сказано.
      `grep pyright` по документам вне вендорного скилла даёт ноль. Отдельно: обычный
      `pyright` печатает «0 errors» одинаково и когда всё хорошо, и когда он
      проанализировал 0 файлов — видимый счётчик даёт только `--stats`.
      Варианты: (а) внести в `AGENTS.md` как обязательный шаг с командой; (б) описать
      в карте скиллов как необязательный инструмент; (в) удалить конфиг.

- [x] **`trocr.py` закреплён как рабочий движок, хотя своя же калибровка велит его снести** ·
      `trocr.py`, `pyrightconfig.json` · важность: средняя
      `calibration.md:76-90`: «хуже и Vision, и Typhoon на этом почерке… Держать не
      рекомендуется; если удалять — снести `scripts/.venv`». Карта знает только «второй
      движок» (`README.md:197`), третьего там нет. При этом правка переводит `trocr.py`
      на общий CLI, а `pyrightconfig.json` делает его venv зависимостью проверки типов
      всего репозитория.
      Варианты: (а) удалить `trocr.py` и venv по рекомендации калибровки; (б) оставить
      и внести третий движок в карту; (в) оставить, но вывести из проверки типов.

- [x] **Сортировка `background` меняет то, что попадает ученице на лист** ·
      `tracker.py:787-790` · важность: средняя
      Единственное изменение в диффе, напрямую видимое в задании: фон закрытой темы
      режется до 4 элементов, и теперь это 4 самых слабых, а не первые по порядку
      словаря. Замысел был про типы и стиль. Правило записано только комментарием в
      коде — `progress-and-spiral.md:296-298` критерий отбора не описывает.
      Варианты: (а) оставить и записать правило в справочник; (б) откатить как выходящее
      за рамки задачи.

---

## Правки — применены

- [x] **Слияние NFC-дублей теряет перевод, транскрипцию и тему** ·
      `tracker.py:317-337` · важность: **высокая**
      `keep = max(existing, candidate, key=(repetitions, mastery))` — проигравшая запись
      выбрасывается целиком. Воспроизведено: полная карточка (`translation: рис`,
      `translit: кхаау`, `topic: 6.1`, `repetitions: 0`) против голого дубля
      (`repetitions: 3`) → после `due` осталась только вторая, перевод и тема потеряны
      безвозвратно, слово ушло в `items_due` с пустым `translation`. Сообщение в stderr
      не называет ни одного затронутого ключа.
      Верно: сливать поля — брать непустое значение из любой записи, а историю по
      максимуму; в stderr называть ключи. Механизм существовал до правки, но до неё
      результат не попадал на диск из читающей команды.

- [x] **`typhoon.py`: любой HTTP-код объявлен «нет модели», тело ошибки выброшено** ·
      `typhoon.py:48-50` · важность: средняя
      Воспроизведено вживую при запущенном ollama и установленной модели: битая картинка
      → `400 Bad Request` → «скорее всего нет модели: `ollama pull …`». Настоящая
      причина (`Failed to load image or audio file`) лежит в теле ответа и выбрасывается.
      Это ровно тот дефект, который правка бралась чинить, воспроизведённый уровнем ниже.
      Верно: отделить 404 от прочих кодов, показывать `e.read()`.

- [x] **Сообщение об ошибке `lookup.py` противоречит своему `--help` и ломает рабочие шаблоны** ·
      `lookup.py:119-124` · важность: средняя
      `--help` обещает regex, и это правда — проверено: `lookup.py "ก(ิ|ี)н"` находит
      слово. Сообщение советует экранировать «круглые скобки, `( ) + |` и прочие
      спецсимволы»; буквальное следование сломает работающий шаблон. Заодно круглые
      скобки названы дважды.
      Верно: назвать конкретную поломку («скобка не закрыта»), а не класс символов.

- [x] **Комментарий обещает `cast` на границе разбора JSON — его там нет** ·
      `tracker.py:60-65` против `load()` · важность: средняя
      Проверено: в `load()` ноль `cast`; `json.load` возвращает `Any`, который молча
      совместим с `Data`. Поля добиваются только на верхнем уровне — записи внутри
      `items`/`mistakes`/`topics` до полного набора не доводятся. Механизм, на котором
      держится вся объявленная выше система TypedDict, назван, но не реализован.
      Верно: либо привести комментарий к тому, что есть, либо поставить `cast` и
      нормализацию в `load()`.

- [x] **Докстринг `new_topic` ссылается на удалённый `DEFAULT_ITEM` и описывает аварию, которой не могло быть** ·
      `tracker.py:211-216` · важность: низкая (нашли все шесть слоёв)
      `grep DEFAULT_ITEM` по репозиторию даёт ровно эти две строки — константы нет, она
      стала `new_item()`, так что «он остаётся словарём» прямо ложно. Само обоснование
      тоже неверно: в старом коде обе ветки `get_topic` переприсваивали
      `attempts`/`core` свежими списками, протечка между темами случиться не могла.
      Верно: описать причину честно — так короче и защищает от будущей правки.

- [x] **`SKIP_DIRS` фильтрует по абсолютному пути** · `lookup.py:44,75` · важность: низкая
      `SKIP_DIRS.intersection(md.parts)` смотрит все компоненты абсолютного пути, а не
      только внутри корня. Любой каталог-предок с именем `.git`, `.venv`, `.cache`,
      `node_modules`, `__pycache__` выключает поиск целиком — и скрипт выдаёт активно
      неверное «нет кандидатов… чтение, скорее всего, неверное». До правки риск был на
      одном имени, список из пяти его расширил. Сейчас на этом расположении репозитория
      не срабатывает.
      Верно: `md.relative_to(root).parts`.

- [x] **Комментарий обещает экономию обхода, которой нет** · `lookup.py:42-45` ·
      важность: низкая
      Фильтр стоит после `rglob`, дерево обходится целиком. Замер: под `.venv` 30 000+
      путей обходятся по-прежнему, сэкономлено чтение 19 файлов из 184, 0.21 c против
      0.18 c. «Обход стоит тысяч файлов» — выигрыш, которого правка не даёт.
      Верно: либо прунинг через `os.walk`, либо комментарий по факту.

- [x] **`ocrcli`: `--dir` и `--compare` не взаимоисключающие, файл молча игнорируется** ·
      `ocrcli.py`, `add_args`/`run` · важность: низкая
      Три способа поданы как равноправные, но проверяются по порядку: вызов
      `typhoon.py страница.jpg --dir /tmp/th` тихо обработает каталог и проигнорирует
      файл. В соседнем `memlog.py:add_target` ровно та же ситуация оформлена
      `add_mutually_exclusive_group`.

- [x] **Осиротевший комментарий про таблицу интервалов** · `tracker.py:174` ·
      важность: низкая
      «Интервалы SM-2 задаются формулой; таблица ниже — только для reps 1 и 2» теперь
      стоит над `new_item()`, к которому не относится.

- [x] **Комментарий в `cmd_mistake` спорит с константой, которой никогда не было** ·
      `tracker.py:894-897` · важность: низкая
      «а не берётся из общей константы… общий пустой словарь копил бы чужие поля» —
      старый код создавал новый литерал на каждый вызов, такой константы не существовало.
      Заодно: у слова есть `new_item()`, у темы `new_topic()`, у ошибки фабрики нет,
      хотя `Mistake` — такой же TypedDict.

- [x] **`ocrcli.py`, `typhoon.py`, `trocr.py` не значатся в карте и в списке слоя ревью** ·
      `README.md:182-200`, `thai-handwriting/SKILL.md:30-58`,
      `skill-review/references/review-layers.md:187` · важность: средняя
      Список скриптов в слое `ревьюер-скриптов` закрытый: `tracker.py`, `thaiocr`,
      `lookup.py`, `brain.py`, `memlog.py`, `clean`, `intake`. Новый `ocrcli.py` владеет
      разбором аргументов обоих дополнительных движков и формально под ревью скриптов не
      попадает. `trocr.py` не упомянут в `SKILL.md` ни разу — правильный способ его
      позвать (интерпретатор из `.venv`) живёт только в докстринге файла.

---

## Отложено

- [x] **`translit` и `voided` пишутся всегда, справочник их не описывает** ·
      `progress-and-spiral.md:70-83,101-104` — расхождение образца схемы с тем, что
      пишет скрипт; справочник объявлен ручным резервом
- [x] **`Example.correct_answer` против `correct` в справочнике** ·
      `progress-and-spiral.md:90,210` — читающая сторона ищет другой ключ; существовало
      до этих изменений
- [x] **Валидный JSON не той формы валит `tracker.py` трейсбеком** · `tracker.py:347-377`
      — `{"items": []}` или `{"items": {"ไป": null}}` дают `AttributeError` вместо
      аккуратного сообщения, которое написано рядом для непарсящегося файла
- [x] **`lookup.py` не ловит `UnicodeDecodeError` и форму `items`** · `lookup.py:60-71`
      — голый трейсбек там, где `tracker.py` печатает внятное сообщение
- [x] **`--compare` на каталоге без `lines.json` — трейсбек** · `ocrcli.py:41-44`
- [x] **`int(f.stem.split("-")[1])` валится на постороннем `line-*.png`** · `ocrcli.py:66`
- [x] **Пакетный прогон OCR обрывается на середине и выглядит завершённым** ·
      `ocrcli.py:72-75`, `sys.exit` внутри `ocr()`
- [x] **Отклонённая попытка закрытия навсегда заводит несуществующую тему** ·
      `tracker.py:678` — опечатка в номере темы становится строкой в `topics`
- [x] **Вендорный `python-best-practices` не внесён в карту скиллов** — закоммичен
      в `aaa9e2a`, вне этих изменений

---

## Отсеяно как шум

- Баннеры `=== … ===` в `ocrcli.run` против «Console output» в `AGENTS.md` — правило
  про мой отчёт о прогонах, а не про формат вывода инструмента; форма унаследована
  дословно из обоих движков.
- «Материал под ревью не покрывает половину незакоммиченного» — вендорный скилл и
  `skills-lock.json` закоммичены в `aaa9e2a`, в незакоммиченном их нет.


---

## Что решено и сделано

- **Запись из читающих команд** → откачено: сохраняет только команда с `writes=True`.
  Слияние NFC живёт в памяти и попадает на диск со следующей пишущей командой.
  Проверено: `due` не меняет файл и не создаёт `.bak`.
- **Потеря полей при слиянии** → историю берём у записи с большей историей, описательные
  поля добираем у второй. Проверено: полная карточка + голый дубль с большей историей →
  остаётся `repetitions: 3` вместе с переводом, транскрипцией и темой.
- **Порог Python** → код возвращён на 3.8: `functools.cache` → `lru_cache(maxsize=None)`,
  `Row` → `typing.Dict`. Заголовки в пяти файлах вернулись на «3.8+». Шесть документов со
  стандартом не тронуты — они снова верны. `pyrightconfig.json`: `pythonVersion: 3.8`.
- **`trocr.py`** → удалён вместе с `scripts/.venv` (876 МБ) по рекомендации
  `calibration.md`; замеры в калибровке сохранены, добавлена дата удаления. `extraPaths`
  из конфига ушёл — проверка типов больше ни от чего внешнего не зависит.
- **`ocrcli.py`** → возвращён в `typhoon.py`: у общего модуля остался один потребитель.
  Вместе с ним ушли импорт через `sys.path` и находка «нет в карте».
- **Сортировка `background`** → оставлена, правило записано в
  `progress-and-spiral.md` (самые слабые по `mastery`, при равенстве — ранняя `due_date`).
- **Проверка типов** → описана в `.codex/skills/README.md`, раздел «Type checking», с
  оговоркой читать счётчик файлов, а не только счётчик ошибок.
- **Список скриптов слоя `ревьюер-скриптов`** → перестал быть закрытым, в него добавлен
  `typhoon.py`.
- Ложные комментарии (`cast` в `load`, `DEFAULT_ITEM`, экономия обхода, общая константа
  в `cmd_mistake`, таблица интервалов, `voided`) приведены к тому, что делает код.
- `typhoon.py`: `HTTPError` больше не выдаёт «нет модели» на любой код — показывает тело
  ответа ollama, совет про `ollama pull` только на 404. Режимы стали взаимоисключающими.
- `lookup.py`: `SKIP_DIRS` считает путь относительно корня; сообщение об ошибке говорит
  про незакрытую скобку и больше не предлагает ломать рабочие regex-шаблоны.

**Проверено после правок:** pyright — 5 файлов, 0 ошибок; трекер прогнан целиком на копии
`progress.json`; страница `brain.py` побайтово совпадает с версией из HEAD; `memlog`,
`lookup`, `typhoon` отвечают как прежде. Боевой `progress.json` не тронут.

---

## Прогон 2 — после применения правок

**Слои:** запущены те же шесть. **Не отработали: пять** — `слепой-охотник`,
`ревьюер-скриптов`, `охотник-за-краями`, `разрыв-проверяемости`, `аудитор-замысла`
оборвались на лимите сессии. Дошёл только `границы-скиллов`.
**Прогон неполный:** регрессии в самих скриптах никто не проверял — только границы и
документацию.

**Итог прогона 2:** 8 правок применено · 2 отложено.

### Применено

- [x] **Отбор `background` описан неверно** · `progress-and-spiral.md:296-303` ·
      важность: средняя. Я написала «до 4 элементов обязаны появиться… самые слабые», но
      в `cmd_due` срок (`if dd > t: continue`) проверяется ДО разделения на `items_due` и
      `background` — кандидатами становятся только подошедшие по SM-2 элементы, и у
      свежезакрытой темы фон может выйти пустым. Текст приведён к коду.
- [x] **Правило слияния дублей устарело в справочнике** · `progress-and-spiral.md:118` ·
      важность: средняя. «Оставляя запись с большей историей» — ровно то поведение,
      которое чинили в первом прогоне как потерю данных. Раздел объявлен ручным резервом,
      то есть по нему человек повторил бы дефект руками.
- [x] **`typhoon.py` внесён в список слоя ревью под ограничением, которое он нарушает** ·
      `review-layers.md:188-199` · важность: средняя. «Полностью офлайн, никаких сетевых
      вызовов» против `urllib` на `127.0.0.1:11434`. Ограничение переформулировано:
      нарушение — вызов в интернет, локальная служба разрешена. То же в
      `skill-doc-standards.md:95`.
- [x] **`description` у `skill-review` перечислял 3 скрипта из 9** · `SKILL.md:5-6` ·
      важность: средняя. Правился парный `references/`, а `SKILL.md` — нет, вопреки
      домашнему стандарту («when you change one, check the other in the same edit»).
      Перечень заменён на `.codex/skills/*/scripts/`, чтобы не рассыхался.
      Подтверждено отдельно: правка меняет условие срабатывания скилла.
- [x] **«По этой же рекомендации» без антецедента** · `calibration.md:87-93` — сама
      рекомендация была вырезана той же правкой; возвращена.
- [x] **Два размера одного venv** · `calibration.md:76,91` — 1,3 ГБ считались с весами
      модели, 876 МБ без; теперь сказано.
- [x] **Мёртвая строка в `.gitignore`** — путь снесённого venv.
- [x] **`pyrightconfig.json` называл источником порога `README.md`**, где версия указана
      только для `tracker.py`; ссылка переведена на `skill-doc-standards.md`.

### Отложено (прогон 2)

- [x] **Карта говорит «Six layers», справочник объявляет семь** — `аудитор-замысла` не
      перечислен в `README.md:317-320`. Существовало до этих изменений.
- [x] **Стандарт для скриптов не знает о проверке типов** — `skill-doc-standards.md`,
      раздел «Scripts», ничего не говорит ни о `pyrightconfig.json`, ни о требовании
      аннотаций. За рамками принятого решения «описать в карте как необязательный
      инструмент».

### Не проверено

Пять слоёв не отработали. **Регрессии в коде скриптов вторым прогоном не искали** —
проверены только границы, документация и согласованность версий. Если правки первого
прогона внесли дефект в `merge_denormalized_keys`, `main` или `typhoon.py`, этот прогон
его не поймал бы.


---

## Разбор отложенного (2026-07-28)

Все 11 отложенных пунктов разобраны по группам; парковка очищена.

### A. Справочник расходился с реальным файлом · `progress-and-spiral.md`

- [x] В образце примеров ошибки стоял ключ `correct`, а скрипт пишет `correct_answer` —
      проверено по боевому `progress.json`: на диске `correct_answer`. Прав был код,
      исправлен образец. Скилл, читавший справочник, искал бы несуществующий ключ.
- [x] В схему `items` добавлен `translit`, в `attempts` — `voided`: оба пишутся всегда,
      но в образце их не было. Раздел объявлен ручным резервом, то есть по нему человек
      завёл бы неполные записи.

### B. Битый трекер объясняется, а не падает трейсбеком

- [x] `tracker.load` проверяет каркас до первого обращения к полям: верхний уровень,
      четыре раздела, каждая запись в них. `[]`, `{"items": []}`, `{"items": {"ไป": null}}`
      дают одно и то же внятное сообщение с указанием на `.bak` и выход с кодом 2 вместо
      `AttributeError`. Проверка вынесена в `check_shape` — иначе сужение типа от
      `isinstance` растекалось бы по всему `load`.
      Добавлен и `except OSError`: файл, который есть, но не открывается.
- [x] `lookup.py` ловит `UnicodeDecodeError` (подкласс `ValueError`, не `OSError`) и
      неверную форму `items`, и продолжает искать по материалам — словарь из трекера там
      подспорье, а не обязательный вход.

### C. Краевые в `typhoon.py`

- [x] `--compare` на каталоге без `lines.json` объясняет, что каталог собирает
      `thaiocr lines`, вместо `FileNotFoundError`. Каталога нет вовсе или в нём нет
      `line-*.png` — тоже отдельные сообщения.
- [x] Посторонний `line-a.png` больше не роняет сравнение на `int()`: `line_index`
      возвращает `None`, файл читается, но помечается как несопоставленный.
- [x] Пакетный прогон не обрывается на одной плохой строке. Ошибка одной картинки
      (`OcrFailed`) собирается в список и печатается в конце, код возврата 1 отличает
      частичный прогон от полного. Обрыв соединения с ollama по-прежнему прекращает всё:
      следующая картинка тоже не прочитается.

### D. Фантомная тема

- [x] `close` по номеру, которого нет ни в темах, ни в элементах, ни в ошибках, теперь
      отказывается и подсказывает `topics` / `import`. Раньше опечатка навсегда заводила
      тему со статусом «нет критерия», снять её можно было только правкой файла руками.

### E. Документация

- [x] Вендорный `python-best-practices` внесён в карту скиллов с пометкой, что его
      исходники не правятся, а заменяются при обновлении.
- [x] «Six layers» → «Seven layers», добавлен `аудитор-замысла` с его условием.
- [x] `skill-doc-standards.md`, раздел «Scripts»: описана проверка типов (необязательная,
      не гейт) и записано правило про 3.8 — обобщённые типы только внутри аннотаций,
      `typing.Dict` и `lru_cache(maxsize=None)` вместо вычисляемых на импорте.

**Проверено:** pyright — 5 файлов, 0 ошибок; все команды трекера на копии
`progress.json`; краевые входы групп B, C, D воспроизведены и дают сообщения вместо
трейсбеков. Боевой `progress.json` не тронут.

---

## Прогон 3 — после коммита 95b3e19

**Слои:** три (`ревьюер-скриптов`, `слепой-охотник`, `границы-и-документация`), все
отработали. Материал — полные тексты пяти скриптов и конфига, а не diff: два
предыдущих прогона правили код многократно.

**Итог:** 3 высоких · 11 средних · 15 низких.

### Высокие

- [x] **Закрытая тема при выросшей сложности не закрывается никогда** ·
      `tracker.py:621-627`, `638-640`, `826-834`
      Блокер «сложность выросла — нужно переподтверждение» уходит в `hard`, а `hard`
      отклоняет попытку: единственный способ переподтвердить сам себя и запрещает.
      Вторая половина: успешный контроль не обновляет `closed_at_difficulty`, поэтому
      блокер висит вечно. Воспроизведено: тема `closed`, `closed_at_difficulty: 4`,
      `meta.difficulty: 6`, все прочие ворота сняты → «не засчитана».
- [x] **`--accuracy 90` вместо `0.9` проходит ворота как успех** · `tracker.py:1454`
      Значение не проверяется на диапазон 0..1. Воспроизведено: `close … --accuracy 90`
      → «Контроль пройден». Обратная опечатка даёт абсурд: `-5` → «результат -500%».
      То же у `set-meta --recent-accuracy`.
- [x] **Английский текст на русской странице композера** · `brain.py:556`, `560`,
      `644-645` — нарушение `AGENTS.md` («everything a human sees … is in Russian»)
      13 карточек «✨ Invent a Структурные technique» и сообщение о сбое буфера обмена.
      Остальная страница переведена, включая успешную ветку того же баннера.

### Средние

- [x] `attempt` молча теряет пример, если не передан `--context` · `tracker.py:1173-1177`
      (`cmd_mistake` рядом гейтит по `--your`/`--correct`)
- [x] `typhoon.py`: `TimeoutError` проходит мимо `OcrFailed` и роняет весь пакет ·
      `typhoon.py:96-107`, `140-155`. Таймаут на запросе к тому же выдаёт «ollama
      недоступен», хотя ollama запущен
- [x] `cmd_import` на не-UTF-8 файле — трейсбек; вежливая ветка в `read_exit_task`
      недостижима, потому что вызывается позже · `tracker.py:1256-1263`
- [x] NFC-слияние обесценивает ручную разметку `core`: ключ в `items` становится NFC,
      в `core` остаётся прежний, ядро молча пересобирается · `tracker.py:327-373`
- [x] NFC-слияние при непустых и разных полях переносит слово в чужую тему ·
      `tracker.py:350-371`
- [x] Guard от опечатки обходится: `record --topic 9.9` и `mistake --topic 8.8`
      заводят тему через `topic_shake` раньше, чем guard успевает сработать
- [x] `resolve` по несуществующему паттерну: код возврата 0 и ротация `.bak`
      (`cmd_attempt` в той же ситуации делает `sys.exit(1)`) · `tracker.py:1022-1029`
- [x] Тема страницы композера не сохраняется: читается `skillkit-theme`, пишется
      `bmad-theme` · `brain.py:278` против `:427`
- [x] `typhoon.py` устроен вокруг `line-*.png`, которые `SKILL.md:61` и
      `calibration.md:70-72` прямо запрещают ему скармливать
- [x] `memlog append`/`set` до `init` — трейсбек · `memlog.py:165`, `:181`
      (потерян при триаже прогона 1 — в парковку не попал)
- [x] Документация против кода: `mistake --topic` объявлен обязательным (не является);
      `subcategory` и `mode` не описаны в схеме; интервалы дрилла 3/7/16 не описаны
      в «формулах — источнике истины»; два разных правила перевода в `resolved`;
      «семь условий» при девяти жёстких воротах; образец вывода `progress` не совпадает

### Низкие

Мёртвое: `MAX_ROUNDS_PER_DAY`, недостижимый фильтр `HEADER_WORDS`, недостижимый
`DRILL_INTERVAL_LONG`, `# noqa: C901` для линтера, которого в проекте нет,
`subcategory` / `target_success` / `audience` / `data-prov` / `Entry.topic` без
потребителя, `resolve` отсутствует в докстринге модуля, `blockers` без guard.

Устаревшие комментарии — **три из них написаны мной в прогонах 1–2**: докстринг
`new_topic` утверждает, что пересборка списков в `get_topic` снята (она на месте);
совет `lookup.py` про непарные `[ ]`, которые до `re.error` не доходят по построению;
обоснование `line_index` описывает файлы, не попадающие в цикл. Плюс наследие:
«средний mastery» над кодом, который его не считает; докстринг `brain.py` про
`customize.toml` и `pick_methods.py`, которых в репозитории нет.

Числа: «Seven skills» при двенадцати (`skill-doc-standards.md:35`); «Шесть режимов»
при семи (`drill-modes.md:3`); сообщение `import` называет одну секцию из двух, причём
ту, которой нет ни в одном из 36 файлов курса.


### Все находки прогона 3 исправлены

**Высокие**

- Блокер `difficulty` переведён в мягкие (`SOFT_BLOCKERS`): попытка закрытия и есть
  требуемое переподтверждение. Успешный контроль переносит `closed_at_difficulty` на
  текущую сложность, поэтому блокер больше не висит вечно. Проверено: тема с
  `closed_at_difficulty: 4` при `difficulty: 6` теперь проходит контроль, планка
  переезжает на 6.
- Доли (`--accuracy`, `--recent-accuracy`) получили собственный тип `share` с проверкой
  0..1. `--accuracy 90` теперь отвергается с подсказкой «85% — это 0.85».
- Страница композера переведена: карточки «Придумать технику: <категория>», сообщения о
  сбое буфера обмена. Ассет `brain-selector.html` перегенерирован, английского в нём нет.

**Средние**

- Слияние NFC переносит ключи в `topics[*].core` (ручная разметка ядра больше не
  обесценивается) и вслух сообщает о столкновении непустых разных тем, не выбирая молча.
- `typhoon.py`: таймаут отделён от «нет соединения» и стал бедой одной картинки; сбои
  `sips` тоже; добавлено предупреждение, что построчный режим противоречит калибровке.
- `cmd_import` объясняет не-UTF-8 и нечитаемый файл вместо трейсбека.
- Опечатка в номере темы закрыта и в `blockers`.
- `resolve` по несуществующему паттерну — код 1 до записи, как в `attempt`.
- `attempt` сохраняет пример по тем же условиям, что `mistake`.
- `check_shape` проверяет `attempts`/`core` на список и попытки на объекты.
- `save()` подметает только временные файлы старше часа — чужую запись больше не роняет.
- `memlog append`/`set` до `init` отвечают сообщением и кодом 2.
- `mistake --topic` стал обязательным — как и было записано в справочнике.
- Тема страницы сохраняется: ключ `localStorage` один и тот же на чтение и запись.

**Низкие**

Убрано мёртвое: `MAX_ROUNDS_PER_DAY`, `HEADER_WORDS` с недостижимым фильтром,
`# noqa: C901`, `data-prov`, `Entry.topic`. Исправлены устаревшие комментарии, включая
три моих собственных (докстринг `new_topic`, совет `lookup` про `[ ]`, обоснование
`line_index`). Числа приведены к фактам: «Семь режимов», «A dozen skills». `resolve`
добавлен в перечень команд модуля. Сообщение `import` называет обе секции критерия.
Докстринг `brain.py` больше не ссылается на `customize.toml` и `pick_methods.py`.
`lookup.py` нормализует и словарь, и материалы, и сам шаблон в NFC.

Справочник дополнен: `mode` и `subcategory` в схеме, интервалы дрилла 3/7, два пути к
`resolved`, два дополнительных основания отказа `close`, исправленный образец вывода
`progress`.

**Проверено:** pyright 5 файлов / 0 ошибок; все 14 команд трекера на копии боевого
`progress.json`; `brain`, `memlog`, `lookup`, `typhoon`. Боевой файл не тронут.
